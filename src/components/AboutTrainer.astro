---
import StatsCounter from './ui/StatsCounter';

const stats = [
  { value: 500, suffix: '+', label: 'Clientes Transformados' },
  { value: 8, suffix: '+', label: 'Años de Experiencia' },
  { value: 100, suffix: '%', label: 'Dedicación' },
  { value: 24, suffix: '/7', label: 'Motivación Continua' }
];
---

<section id="about-trainer" class="relative w-full h-full min-h-full overflow-hidden flex">
  <div class="relative z-10 grid grid-cols-1 md:grid-cols-[35%_65%] h-full min-h-full flex-1">
    <!-- Left: Image Panel (35%) -->
    <div class="relative h-full min-h-full order-1 hidden md:block">
      <img 
        src="/jairo.jpg" 
        alt="Jairo entrenando" 
        class="absolute inset-0 w-full h-full object-cover object-center" 
        loading="eager"
        fetchpriority="high"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-black/20 to-transparent"></div>
    </div>

    <!-- Right: Bio + Stats (65%) -->
    <div class="order-2 bg-gray-900 h-full min-h-full flex items-center px-6 sm:px-10 lg:px-16 py-10">
      <div class="max-w-3xl mx-auto text-center">
        <!-- Brand images instead of text heading -->
        <div class="flex flex-col items-center justify-center mb-4 sm:mb-6">
          <img src="/logo-blanco.png" alt="Jairo Traine" class="about-logo w-auto hidden sm:block" width="320" height="160" />
          <img src="/nombre-blanco.png" alt="Jairo Traine" class="about-wordmark w-auto hidden sm:block" width="360" height="64" />
        </div>

        <p class="text-base sm:text-xl lg:text-3xl text-gray-300 leading-relaxed">
          Soy tu <strong class="text-primary">mayor fan</strong>. Entrenador personal certificado, obsesionado con el detalle y con conseguir resultados reales. Mi método combina fuerza inteligente, nutrición sencilla y una mentalidad que no se rinde.
        </p>

        <div class="mt-8 sm:mt-10 grid grid-cols-2 gap-3 sm:gap-4">
          <div class="rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
            <p class="text-sm sm:text-base text-gray-400">Especialidad</p>
            <p class="text-white font-bold text-base sm:text-xl">Recomposición corporal</p>
          </div>
          <div class="rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
            <p class="text-sm sm:text-base text-gray-400">Ciudad</p>
            <p class="text-white font-bold text-base sm:text-xl">Medellín, CO</p>
          </div>
        </div>

        <div class="mt-8 sm:mt-12">
          <StatsCounter stats={stats} client:visible />
        </div>

        <div class="mt-8 sm:mt-12 flex flex-wrap gap-3 sm:gap-4 justify-center">
          <a href="#servicios" class="inline-flex items-center px-6 py-4 sm:px-8 sm:py-5 rounded-lg bg-primary text-gray-900 text-base sm:text-lg font-black shadow-md hover:bg-primary-light transition-colors">
            Conoce mis servicios
            <svg class="w-5 h-5 sm:w-6 sm:h-6 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
          <a href="/contacto" class="inline-flex items-center px-6 py-4 sm:px-8 sm:py-5 rounded-lg border-2 border-white/30 text-white text-base sm:text-lg font-bold hover:bg-white hover:text-gray-900 transition-colors">
            Agenda una consulta
          </a>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* Base (móvil): ocultar imágenes de marca para ganar espacio */
    .about-logo { height: 6rem; margin-top: -4%; margin-bottom: -4%; }
    .about-wordmark { height: 2.5rem; margin-top: -4%; margin-bottom: -4%; }

    @media (min-width: 640px) {
      .about-logo { height: 10rem; margin-top: -8%; margin-bottom: -8%; }
      .about-wordmark { height: 4rem; margin-top: -8%; margin-bottom: -8%; }
    }
    @media (min-width: 768px) {
      .about-logo { height: 16rem; margin-top: -12%; margin-bottom: -12%; }
      .about-wordmark { height: 7rem; margin-top: -8%; margin-bottom: -8%; }
    }
    @media (min-width: 1024px) {
      .about-logo { height: 20rem; margin-top: -10%; margin-bottom: -10%; }
      .about-wordmark { height: 12rem; margin-top: -10%; margin-bottom: -10%; }
    }
  </style>
</section>
