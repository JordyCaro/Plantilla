---
const features = [
  {
    icon: '💪',
    title: 'MUSCLES BUILDING',
    description: 'Entrenamiento diseñado para desarrollar masa muscular y fuerza de forma efectiva y segura.',
    gradient: 'from-primary-dark to-primary'
  },
  {
    icon: '🎯',
    title: 'NUTRICIÓN PERSONALIZADA',
    description: 'Planes alimenticios adaptados a tus objetivos específicos para maximizar tus resultados.',
    gradient: 'from-primary to-primary-light'
  },
  {
    icon: '🔥',
    title: 'MOTIVACIÓN CONTINUA',
    description: 'Apoyo constante y seguimiento personalizado para mantener tu compromiso y alcanzar tus metas.',
    gradient: 'from-primary-dark to-primary-light'
  }
];
---

<section id="why-choose-us" class="py-20 sm:py-24 lg:py-32 px-4 sm:px-6 lg:px-8 bg-white dark:bg-gray-900">
  <div class="w-full max-w-[1400px] mx-auto">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl sm:text-5xl lg:text-6xl font-black text-gray-900 dark:text-white mb-4">
        ¿POR QUÉ <span class="text-primary-dark dark:text-primary-light">ELEGIRME?</span>
      </h2>
      <p class="text-lg sm:text-xl text-gray-600 dark:text-gray-400 max-w-3xl mx-auto">
        Un enfoque profesional y personalizado para alcanzar tus objetivos de fitness
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
      {features.map((feature, index) => (
        <div 
          class="feature-card group relative overflow-hidden rounded-2xl bg-white dark:bg-gray-800 border-2 border-gray-200 dark:border-gray-700 hover:border-primary-dark dark:hover:border-primary-light transition-all duration-300 p-8 hover:shadow-2xl hover:shadow-primary-dark/20 transform hover:-translate-y-2"
        >
          <!-- Icon Circle -->
          <div class={`w-20 h-20 rounded-full bg-gradient-to-br ${feature.gradient} flex items-center justify-center mb-6 transform group-hover:scale-110 group-hover:rotate-3 transition-transform duration-300 shadow-lg mx-auto`}>
            <span class="text-4xl">{feature.icon}</span>
          </div>

          <!-- Content -->
          <h3 class="text-2xl font-black text-gray-900 dark:text-white mb-4 text-center group-hover:text-primary-dark dark:group-hover:text-primary-light transition-colors">
            {feature.title}
          </h3>
          <p class="text-gray-600 dark:text-gray-300 leading-relaxed text-center mb-6">
            {feature.description}
          </p>

          <!-- Button -->
          <div class="text-center">
            <a 
              href="#servicios"
              class="inline-flex items-center text-primary-dark dark:text-primary-light font-bold hover:gap-3 transition-all duration-300"
            >
              <span>CONOCE MÁS</span>
              <svg class="w-5 h-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

