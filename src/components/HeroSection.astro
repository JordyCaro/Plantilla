---
---

<section class="relative h-screen flex items-center justify-center overflow-hidden mt-0">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0 z-0">
    <img
      src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?auto=format&fit=crop&q=80&w=1920"
      alt="Fitness Training Background"
      class="w-full h-full object-cover"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-black/60 to-black/80"></div>
  </div>

  <!-- Animated Shapes -->
  <div class="absolute inset-0 z-0 overflow-hidden">
    <div class="absolute top-20 left-10 w-72 h-72 bg-primary-dark/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-20 right-10 w-96 h-96 bg-primary-light/10 rounded-full blur-3xl animate-pulse delay-150"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Main Heading -->
    <h1 class="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-black text-white mb-6 leading-tight">
      <span class="block">Transforma</span>
      <span class="block text-primary-light">Tu Cuerpo</span>
      <span class="block">Tu Mente</span>
    </h1>

    <!-- Tagline -->
    <div class="mb-8">
      <p class="text-3xl sm:text-4xl lg:text-5xl font-bold text-primary-light mb-4 animate-pulse">
        Soy tu mayor fan
      </p>
      <p class="text-xl sm:text-2xl text-gray-200 max-w-3xl mx-auto">
        Entrenamiento personalizado diseñado para ti. Resultados reales, transformaciones duraderas.
      </p>
    </div>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mt-12">
      <a 
        href="/contacto" 
        class="group relative inline-flex items-center justify-center px-10 py-5 text-xl font-bold text-black bg-primary-light hover:bg-primary rounded-full transition-all duration-300 transform hover:scale-105 shadow-2xl hover:shadow-primary-light/50"
      >
        <span>Empieza Ahora</span>
        <svg class="w-6 h-6 ml-3 group-hover:translate-x-2 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
      <a 
        href="#servicios" 
        class="inline-flex items-center justify-center px-10 py-5 text-xl font-bold text-white border-3 border-white hover:bg-white hover:text-black rounded-full transition-all duration-300 transform hover:scale-105"
      >
        Ver Servicios
      </a>
    </div>

    <!-- Stats Bar -->
    <div class="grid grid-cols-3 gap-8 mt-20 max-w-3xl mx-auto">
      <div class="text-center border-r border-gray-600">
        <div class="text-4xl font-black text-primary-light mb-2">+500</div>
        <div class="text-sm text-gray-300 uppercase tracking-wider">Transformaciones</div>
      </div>
      <div class="text-center border-r border-gray-600">
        <div class="text-4xl font-black text-primary-light mb-2">+8</div>
        <div class="text-sm text-gray-300 uppercase tracking-wider">Años Experiencia</div>
      </div>
      <div class="text-center">
        <div class="text-4xl font-black text-primary-light mb-2">100%</div>
        <div class="text-sm text-gray-300 uppercase tracking-wider">Dedicación</div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-10 left-1/2 transform -translate-x-1/2 z-10 animate-bounce">
    <svg class="w-8 h-8 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
