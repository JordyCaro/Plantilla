---
const transformations = [
  {
    id: 1,
    nombre: "María G.",
    objetivo: "Pérdida de Peso",
    resultado: "-20kg en 6 meses",
    imagen: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&q=80&w=400",
    testimonio: "Jairo cambió mi vida completamente. No solo perdí peso, gané confianza."
  },
  {
    id: 2,
    nombre: "Carlos R.",
    objetivo: "Ganancia Muscular",
    resultado: "+12kg de músculo",
    imagen: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&q=80&w=400",
    testimonio: "El entrenamiento con Jairo es intenso pero los resultados hablan por sí solos."
  },
  {
    id: 3,
    nombre: "Laura M.",
    objetivo: "Cambio de Hábitos",
    resultado: "Estilo de vida saludable",
    imagen: "https://images.unsplash.com/photo-1544005313-94ddf0286df2?auto=format&fit=crop&q=80&w=400",
    testimonio: "Más que un entrenador, es un mentor que me enseñó a vivir saludable."
  },
  {
    id: 4,
    nombre: "Jorge D.",
    objetivo: "Preparación Competencia",
    resultado: "1er lugar local",
    imagen: "https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?auto=format&fit=crop&q=80&w=400",
    testimonio: "Jairo me preparó para mi primera competencia y logré el primer lugar."
  },
  {
    id: 5,
    nombre: "Sofía P.",
    objetivo: "Recomposición Corporal",
    resultado: "-8kg grasa / +4kg músculo",
    imagen: "https://images.unsplash.com/photo-1547425260-76bcadfb4f2c?auto=format&fit=crop&q=80&w=400",
    testimonio: "Aprendí a entrenar y comer mejor. Los cambios se notaron desde el primer mes."
  }
];
---

<section id="transformations" class="transformations-section">
  <div class="transformations-container">
    <!-- Section Header -->
    <div class="transformations-header">
      <h2 class="transformations-title">
        TRANSFORMACIONES <span class="transformations-title-accent">INCREÍBLES</span>
      </h2>
      <p class="transformations-subtitle">
        Estas son solo algunas de las historias de éxito de personas que confiaron en el proceso
      </p>
    </div>

    <!-- Cards Grid -->
    <div class="transformations-grid">
      {transformations.map((transformation) => (
        <div class="transformation-card">
          <div class="transformation-card-header">
            <div class="transformation-card-avatar">
              <img src={transformation.imagen} alt={transformation.nombre} />
            </div>
            <div class="transformation-card-info">
              <p class="transformation-card-name">{transformation.nombre}</p>
              <p class="transformation-card-objective">{transformation.objetivo}</p>
            </div>
          </div>
          <div class="transformation-card-content">
            <h4 class="transformation-card-result">{transformation.resultado}</h4>
            <p class="transformation-card-testimonial">"{transformation.testimonio}"</p>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA Button -->
    <div class="transformations-cta">
      <a href="/contacto" class="transformations-button">
        <span>Comienza tu transformación</span>
        <svg class="transformations-button-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  /* Transformations Section - Sin scroll interno, todo debe caber */
  .transformations-section {
    position: relative;
    width: 100%;
    min-height: 100svh;
    display: flex;
    align-items: stretch;
    justify-content: stretch;
    overflow: hidden;
    box-sizing: border-box;
    background-color: #111827;
  }

  .transformations-container {
    position: relative;
    z-index: 10;
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
    height: 100svh;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    padding: var(--header-h, 64px) clamp(1rem, 4vw, 2rem) clamp(0.125rem, 0.5svh, 0.5rem);
    padding-top: var(--header-h, 64px);
    padding-bottom: clamp(0.125rem, 0.5svh, 0.5rem);
    box-sizing: border-box;
  }

  /* Header */
  .transformations-header {
    text-align: center;
    padding: clamp(0.75rem, 2svh, 1.5rem) 0;
    flex-shrink: 0;
    box-sizing: border-box;
  }

  .transformations-title {
    font-size: clamp(2rem, 7vw, 3.75rem);
    font-weight: 900;
    color: #ffffff;
    margin: 0 0 clamp(0.5rem, 1.5svh, 1rem) 0;
    line-height: 1.1;
  }

  .transformations-title-accent {
    color: #4FFF85;
  }

  .transformations-subtitle {
    font-size: clamp(0.9375rem, 2.2svh, 1.25rem);
    color: #d1d5db;
    max-width: 48rem;
    margin: 0 auto;
    line-height: 1.5;
  }

  /* Grid */
  .transformations-grid {
    flex: 1;
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: clamp(0.75rem, 2svh, 1.25rem);
    overflow-y: auto;
    overflow-x: hidden;
    scrollbar-width: none;
    -ms-overflow-style: none;
    -webkit-overflow-scrolling: touch;
    padding: clamp(0.5rem, 1.5svh, 1rem) 0;
    box-sizing: border-box;
    min-height: 0;
    align-content: start;
  }

  .transformations-grid::-webkit-scrollbar {
    display: none;
  }

  @media (min-width: 640px) {
    .transformations-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: clamp(1rem, 2.5svh, 1.5rem);
    }
  }

  @media (min-width: 1024px) {
    .transformations-grid {
      grid-template-columns: repeat(12, 1fr);
      grid-auto-rows: min-content;
    }
  }

  /* Cards */
  .transformation-card {
    padding: clamp(0.75rem, 2svh, 1.25rem);
    border-radius: 0.9rem;
    background-color: #0b0b0b;
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
    color: #ffffff;
    display: flex;
    flex-direction: column;
    gap: clamp(0.5rem, 1.5svh, 0.75rem);
    box-sizing: border-box;
    height: auto;
    min-height: auto;
    overflow: visible;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  @media (min-width: 1024px) {
    .transformation-card:nth-child(1) {
      grid-column: span 6;
      grid-row: span 1;
    }
    .transformation-card:nth-child(2) {
      grid-column: span 3;
      grid-row: span 1;
    }
    .transformation-card:nth-child(3) {
      grid-column: span 3;
      grid-row: span 2;
      background-color: #0d0d0d;
    }
    .transformation-card:nth-child(4) {
      grid-column: span 3;
      grid-row: span 1;
      background-color: #0d0d0d;
    }
    .transformation-card:nth-child(5) {
      grid-column: span 6;
      grid-row: span 1;
    }
  }

  .transformation-card-header {
    display: flex;
    align-items: center;
    gap: clamp(0.75rem, 1.8svh, 1rem);
    flex-shrink: 0;
  }

  .transformation-card-avatar {
    flex-shrink: 0;
  }

  .transformation-card-avatar img {
    border-radius: 50%;
    width: clamp(2rem, 4.5svh, 2.5rem);
    height: clamp(2rem, 4.5svh, 2.5rem);
    object-fit: cover;
    border: 2px solid rgba(255, 255, 255, 0.5);
  }

  .transformation-card-info {
    flex: 1;
    min-width: 0;
  }

  .transformation-card-name {
    color: #ffffff;
    font-size: clamp(0.8125rem, 1.8svh, 0.9375rem);
    font-weight: 700;
    margin: 0 0 0.125rem 0;
  }

  .transformation-card-objective {
    color: #4FFF85;
    font-size: clamp(0.6875rem, 1.6svh, 0.8125rem);
    margin: 0;
  }

  .transformation-card-content {
    display: flex;
    flex-direction: column;
    gap: clamp(0.375rem, 1.2svh, 0.625rem);
    flex: 1;
    min-height: 0;
  }

  .transformation-card-result {
    font-size: clamp(0.9375rem, 2.2svh, 1.125rem);
    color: #ffffff;
    font-weight: 800;
    line-height: 1.3;
    margin: 0;
    flex-shrink: 0;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .transformation-card-testimonial {
    font-size: clamp(0.8125rem, 1.8svh, 0.875rem);
    color: #e5e7eb;
    opacity: 0.9;
    line-height: 1.4;
    margin: 0;
    word-wrap: break-word;
    overflow-wrap: break-word;
    white-space: normal;
    overflow: visible;
    text-overflow: clip;
  }

  /* CTA Button */
  .transformations-cta {
    text-align: center;
    padding: clamp(0.75rem, 2svh, 1.5rem) 0;
    flex-shrink: 0;
    box-sizing: border-box;
  }

  .transformations-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: clamp(0.875rem, 2.5svh, 1rem) clamp(2rem, 5vw, 2.5rem);
    font-size: clamp(1rem, 2.5svh, 1.125rem);
    font-weight: 700;
    border-radius: 0.5rem;
    background-color: #4FFF85;
    color: #1a1a1a;
    box-shadow: 0 4px 12px rgba(79, 255, 133, 0.3);
    transition: all 0.3s ease;
    text-decoration: none;
    transform: scale(1);
  }

  .transformations-button:hover {
    background-color: #7CFF9F;
    box-shadow: 0 6px 16px rgba(79, 255, 133, 0.4);
    transform: scale(1.05);
  }

  .transformations-button-icon {
    width: clamp(1.25rem, 3svh, 1.5rem);
    height: clamp(1.25rem, 3svh, 1.5rem);
    margin-left: 0.5rem;
  }

  /* Responsive - Mobile */
  @media (max-width: 639px) {
    .transformations-container {
      padding: 0 clamp(1rem, 4vw, 1.5rem) clamp(0.125rem, 0.5svh, 0.5rem);
      padding-top: var(--header-h, 64px);
      height: 100svh;
      min-height: 100svh;
    }
    .transformations-header {
      padding: clamp(0.5rem, 1.5svh, 1rem) 0;
      flex-shrink: 0;
    }
    .transformations-title {
      font-size: clamp(1.75rem, 6vw, 2.5rem);
      margin-bottom: clamp(0.375rem, 1svh, 0.75rem);
    }
    .transformations-subtitle {
      font-size: clamp(0.875rem, 2svh, 1rem);
    }
    .transformations-grid {
      flex: 1;
      gap: clamp(0.75rem, 2svh, 1rem);
      padding: clamp(0.25rem, 1svh, 0.75rem) 0;
      grid-auto-rows: min-content;
      align-content: start;
      min-height: 0;
      overflow-y: auto;
      overflow-x: hidden;
    }
    .transformation-card {
      padding: clamp(0.75rem, 1.8svh, 1rem);
      gap: clamp(0.5rem, 1.2svh, 0.625rem);
      height: auto;
      min-height: auto;
      max-height: none;
    }
    .transformation-card-header {
      gap: 0.75rem;
    }
    .transformation-card-avatar img {
      width: clamp(1.75rem, 4svh, 2.25rem);
      height: clamp(1.75rem, 4svh, 2.25rem);
    }
    .transformation-card-content {
      gap: clamp(0.375rem, 1svh, 0.5rem);
    }
    .transformation-card-result {
      font-size: clamp(0.875rem, 2svh, 1rem);
      line-height: 1.2;
    }
    .transformation-card-testimonial {
      font-size: clamp(0.75rem, 1.6svh, 0.8125rem);
      line-height: 1.3;
    }
    .transformations-cta {
      padding: clamp(0.5rem, 1.5svh, 1rem) 0;
      flex-shrink: 0;
    }
  }

  /* Responsive - Tablet */
  @media (min-width: 640px) and (max-width: 1023px) {
    .transformations-header {
      padding: clamp(1rem, 2.5svh, 1.5rem) 0;
    }
    .transformations-title {
      font-size: clamp(2.5rem, 8vw, 3.5rem);
    }
    .transformations-subtitle {
      font-size: clamp(1rem, 2.5svh, 1.125rem);
    }
  }

  /* Responsive - Desktop */
  @media (min-width: 1024px) {
    .transformations-header {
      padding: clamp(1.25rem, 3svh, 2rem) 0;
    }
    .transformations-title {
      font-size: clamp(3rem, 9vw, 3.75rem);
    }
    .transformations-subtitle {
      font-size: clamp(1.125rem, 2.8svh, 1.25rem);
    }
  }

  /* Fallback para pantallas con poca altura */
  @media (max-height: 600px) {
    .transformations-header {
      padding: clamp(0.5rem, 1.2svh, 0.75rem) 0;
    }
    .transformations-title {
      font-size: clamp(1.5rem, 5vw, 2rem);
      margin-bottom: clamp(0.25rem, 0.8svh, 0.5rem);
    }
    .transformations-subtitle {
      font-size: clamp(0.8125rem, 1.8svh, 0.95rem);
    }
    .transformations-grid {
      gap: clamp(0.5rem, 1.2svh, 0.75rem);
      padding: clamp(0.25rem, 0.8svh, 0.5rem) 0;
    }
    .transformation-card {
      padding: clamp(0.75rem, 1.8svh, 1rem);
      gap: clamp(0.5rem, 1.2svh, 0.75rem);
    }
    .transformation-card-result {
      font-size: clamp(0.875rem, 2svh, 1rem);
    }
    .transformation-card-testimonial {
      font-size: clamp(0.75rem, 1.8svh, 0.875rem);
    }
    .transformations-cta {
      padding: clamp(0.5rem, 1.2svh, 0.75rem) 0;
    }
  }
</style>
